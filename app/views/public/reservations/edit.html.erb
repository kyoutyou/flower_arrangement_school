<%= @date%>
<%= form_with(model: @reservation,url: public_reservation_path(@reservation), local: true) do |form| %>

  <div class="time">
    <%= form.label :lesson_datetime, 'レッスン日' %>
    <%= form.date_select :lesson_datetime %>
  </div>

  <div class="submit">
    <%= form.submit '変更' %>
  </div>

<% end %>


